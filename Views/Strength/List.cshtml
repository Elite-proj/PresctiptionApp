@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewBag.Title = "Medication Strengths";
    Layout = null;
}

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - E-Prescription</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" />
    <script src="~/lib/jquery/dist/jquery.js"></script>
</head>

<div class="form-group">
    @if (ViewBag.Strengths.Count > 0)
    {
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Strength</th>
                    <th>Status</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (StrengthVM list in ViewBag.Strengths)
                {
                    <tr>
                        <td>@list.description</td>
                        <td>@list.Status</td>
                        <td><a class="btn btn-primary" asp-action="Edit" asp-controller="Strength" asp-route-id="@list.strengthID">Edit</a></td>
                    </tr>
                }

            </tbody>
        </table>
    }
    else if (ViewBag.Strengths.Count == 0)
    {
        <h4 class="bg-warning text-center text-white p-2">No medication strengths Records.</h4>
    }


    <a asp-action="Add" asp-controller="Strength" class="btn btn-primary">Add new strengths</a>
</div>
