@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewBag.Title = "Dosage Form List";
    Layout = "_AdminLayout";
}

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - E-Prescription</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" />
    <script src="~/lib/jquery/dist/jquery.js"></script>
</head>

<div class="form-group">
    @if (ViewBag.Dosages.Count > 0)
    {
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Dosage Form</th>
                    <th>Status</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (DosageVM list in ViewBag.Dosages)
                {
                    <tr>
                        <td>@list.DosageDescription</td>
                        <td>@list.Status</td>
                        <td><a class="btn btn-primary" asp-action="Edit" asp-controller="DosageForm" asp-route-id="@list.DosageID">Edit</a></td>
                    </tr>
                }

            </tbody>
        </table>
    }
    else if (ViewBag.Dosages.Count == 0)
    {
        <h4 class="bg-warning text-center text-white p-2">No dosage form Records.</h4>
    }


    <a asp-action="Add" asp-controller="DosageForm" class="btn btn-primary">Add new dosage form</a>
</div>
